#include<stdio.h>
#include<conio.h>

struct Node{
int data;
struct Node *next;
};
struct SinglyLinkedList{
struct Node *head,*tail;

void insert()
{
 if(!head)
 {

 }
}
};

int main()
{
    return(0);
}
